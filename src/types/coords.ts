export type Coords = {
  lat: number;
  lng: number;
  name: string;
  address: string; // Place address
  rating: string | number; // Rating (optional)
  placeId: string; // Unique place ID
  website: string; // Website (optional)
};
